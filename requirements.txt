# --- Core runtime (required to run the app with stubs) ---
gradio==6.17.3        # if a Spaces build rejects this, drop to latest 5.x
pydantic==2.9.2
python-dotenv==1.0.1
requests==2.32.3
gTTS==2.5.3            # lightweight TTS fallback for the voice reply
edge-tts==7.2.8        # Kenyan Swahili/English neural voices

# --- Real-model integration ---
cohere==7.0.3           # Tiny Aya translation + localization (hosted Chat API)
huggingface_hub==1.16.1 # HF Inference ASR (Whisper/MMS) for speech-to-text
# transformers==4.46.0  # uncomment for local ASR / MMS-TTS
# torch==2.4.1
# accelerate==1.0.1
